{
  "name": "@modelsjs/request",
  "version": "0.0.1",
  "dependencies": {
    "@doc-tools/docs": "^1.27.0"
  },
  "scripts": {
    "lint": "yfm -i . -o $LINT_ROOT --lang en",
    "build": "yfm -i . -o $BUILD_DIR --output-format md --add-map-file --lang en --lint-disabled"
  }
}
